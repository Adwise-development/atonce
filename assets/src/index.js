/**
 * Fallback entry point dla webpacka (blueprint bez bloków).
 * Webpack wymaga min. 1 entry — ten plik gwarantuje build bez błędów,
 * dopóki nie pojawi się pierwszy blok w "blocks/<dir>/index.js".
 * Możesz tu wstawić globalny JS theme'u (np. init animacji) jeśli potrzeba.
 */
